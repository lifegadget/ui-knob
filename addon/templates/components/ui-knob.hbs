<knob id="{{elementId}}" class='{{if class (concat class ' ')}}ui-knob' style={{stylist}}>

  {{yield}}

  <relativity-frame>
    <canvas
      id="knob-{{elementId}}"
      role="input"
      width="400"
      height="400"
    ></canvas>
    {{text-input value=value onBlur=(mut value)}}
  </relativity-frame>

</knob>
