<knob
  id="{{elementId}}"
  class='{{if class (concat class ' ')}}ui-knob{{if isFocused ' focused'}}'
  style={{stylist}}
  tabindex=0
  onfocus={{action 'onFocus' true}}
  onblur={{action 'onFocus' false}}
>

  {{yield}}

  <relativity-frame>
    {{ui-knob-arc
      width=width
      height=height
      value=value
      cursor=cursor
      min=min
      max=max
      angleArc=angleArc
      angleOffset=angleOffset

      selectedColor=selectedColor
      unselectedColor=unselectedColor
    }}

  </relativity-frame>

</knob>
