<svg class="ui-knob-background{{classy class}}" width={{width}} height={{height}}>
  <g class='background-circle' >
    <circle cx={{cx}} cy={{cy}} r={{cx}} data-color={{color}} style="fill: {{color}}"/>
  </g>
</svg>

{{yield}}
