<svg
  id={{elementId}}
  class="ui-knob-arc"
  width={{width}}
  height={{height}}
>
  <g
    class='arcs'
    transform="translate({{transformSize}},{{transformSize}}){{if _flip ' scale(-1, -1)'}}"
  >
    <path
      d={{arc}}
      class='unselected'
      style={{_knobUnselectedStyle}}
    ></path>
    <path
      class='selected'
      d={{selectedArc}}
      style={{_knobSelectedStyle}}
    ></path>
  </g>

</svg>
